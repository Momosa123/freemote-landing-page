import React from "react";

export default function HeroSection (){

  return(
    <div id="hero" className="hero-section">
    
      <h2 className="hero-heading">We do websites right</h2>
    </div>
  )
}